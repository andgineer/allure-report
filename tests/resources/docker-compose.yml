version: '3.8'

services:
  allure:
    build:
      context: ../../
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ../../allure-results:/allure-results:rw
      - ./website-source:/website-source:rw
      - ./temp/allure-report:/allure-report:rw
#      - ../../src/:/app/:ro
#      - ../../templates:/app/templates:ro
